# 安装步骤

1、安装etcd (由独立脚本安装)
2、安装flannel（由独立脚本安装）
3、安装docker（由独立脚本安装）
4、基础node配置（包括设置修改） tools_install.sh

5、安装master节点(sh master_install.sh)
  a、生成公用证书 certificate_install.sh
  b、生成公用配置文件 config_install.sh
  c、修改api_server controller-manager scheduler kubelet kube-proxy配置和启动配置
  d、启动以上服务
6、安装node节点 (sh node_install.sh)
  a、安装公共证书和配置文件
  b、修改kubelet kube-proxy配置和启动配置
  c、启动以上服务

